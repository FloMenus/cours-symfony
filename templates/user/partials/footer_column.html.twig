<div>
    <p class="font-bold text-slatebrand-900">{{ column.title }}</p>
    <ul class="mt-3 space-y-2">
        {% for link in column.links %}
            <li><a href="#" class="hover:text-coral-500">{{ link }}</a></li>
        {% endfor %}
    </ul>
</div>
